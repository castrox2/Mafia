Electron-specific assets:

- `Mafia-Icon.png` used by `main.js` for dev/runtime window icon.
- `Mafia-Icon.ico` recommended for Windows `.exe` packaging.
- `icons/*` for tray/app/platform icon variants.

Folder layout:

- `icons/`

Keep Electron-only assets here, separate from Client web assets.
